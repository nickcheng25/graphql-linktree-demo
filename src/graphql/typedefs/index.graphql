
# Custom Scalars
scalar Date

type Query {
  # Regular Links
  regularLinks: [RegularLink]!
  regularLink(id: ID!): RegularLink!

#   # Show Links
#   showLinks: [ShowLinks]!
#   showLinks(id: ID!): ShowLinks

#   # Music Player Links
#   musicLinks: [MusicLinks]!
#   musicLinks(id: ID!): MusicLinks
}

type Mutation {
  # Link
  createRegularLink(link: String!): RegularLink!
  updateRegularLink(id: ID!, link: String!): RegularLink!
}